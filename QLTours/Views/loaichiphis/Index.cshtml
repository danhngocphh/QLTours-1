@model IEnumerable<QLTours.Models.loaichiphi>

@{
    ViewBag.Title = "Index";
}

<h2>Loại chi phí</h2>

<p>
    <h3 class="iop">
        <button class="btn btn-warning" style="width: 200px"> @Html.ActionLink("Thêm loại chi phí", "Create") </button>
    </h3>

</p>
<table class="table table-striped table-bordered table-hover">
    <tr>
        <th width="3%" align="left">
            STT
        </th>
        <th width="15%" align="left">
            Tên loại chi phí
        </th>
        <th width="60%" align="left">
            Mô tả
        </th>
        <th width="20%" align="left"></th>
    </tr>
@{int counter = 0; }
@foreach (var item in Model) {
    <tr>
        <td>
            @(++counter)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Ten)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Mota)
        </td>
        <td>
            <button class="btn btn-info"> @Html.ActionLink("Edit", "Edit", new { id = item.Id }) </button>
            <button class="btn btn-info"> @Html.ActionLink("Details", "Details", new { id = item.Id }) </button>
            <button class="btn btn-info"> @Html.ActionLink("Delete", "Delete", new { id = item.Id }) </button>
        </td>
    </tr>
}

</table>
